# 社交媒体舆情分析系统 - 系统设计报告

**项目名称：** 社交媒体舆情分析系统
**平台：** 华为云 GaussDB(for MySQL)
**完成日期：** 2025年
**负责人：** 同学A（系统功能设计50%）| 同学B（数据库设计50%）

---

## 目录
1. [系统需求分析](#一系统需求分析)
2. [系统功能设计](#二系统功能设计)
3. [数据库概念模式设计](#三数据库概念模式设计)
4. [数据库逻辑模式设计](#四数据库逻辑模式设计)
5. [后续规划](#五后续规划)

---

## 一、系统需求分析

### 1.1 项目概述

社交媒体舆情分析系统是一个数据管理和舆情分析平台，基于华为云GaussDB(for MySQL)数据库，支持用户生成内容（UGC）的存储、分析和挖掘。

**核心功能：**
- 用户、帖子、评论数据管理
- 话题标签自动提取与追踪
- 情感倾向自动分析（正面/中立/负面）
- 热点话题看板、舆情趋势、情感分布分析
- 关键词预警查询

### 1.2 功能需求

| 功能模块       | 主要功能                         | 优先级 |
| -------------- | -------------------------------- | ------ |
| **用户管理**   | 用户注册、登录、禁用、删除       | P0     |
| **内容管理**   | 发布/查询帖子、评论；级联删除    | P0     |
| **话题管理**   | 自动提取话题、话题统计           | P0     |
| **情感分析**   | 自动分析情感倾向、生成情感记录   | P0     |
| **热点分析**   | 热点话题排行、舆情趋势、情感分布 | P1     |
| **关键词预警** | 检测敏感词、预警查询             | P1     |

### 1.3 业务约束

- 用户名、邮箱唯一，必须有效
- 帖子/评论必须关联有效用户
- 删除用户时级联删除其帖子和评论
- 新增帖子自动提取话题、分析情感

---

## 二、系统功能设计

### 2.1 系统架构

```mermaid
graph TD
    A["用户/应用层<br/>数据查询、报表展示、实时监控"] -->|数据请求| B["应用服务层<br/>用户服务 | 内容服务 | 分析服务 | 查询服务"]
    B -->|SQL操作| C["数据访问层<br/>CRUD操作、复杂查询、事务管理"]
    C -->|数据存取| D["GaussDB数据库<br/>表 | 索引 | 触发器 | 存储过程"]
    D -->|数据返回| C
    C -->|查询结果| B
    B -->|结果展示| A
  
    style A fill:#e1f5ff
    style B fill:#f3e5f5
    style C fill:#ede7f6
    style D fill:#fce4ec
```

### 2.2 核心功能流程

#### 流程1：发布帖子

```mermaid
sequenceDiagram
    participant 用户
    participant Controller
    participant Service
    participant Database
    participant Trigger

    用户->>Controller: 发布帖子(内容)
    Controller->>Service: 验证请求
    Service->>Database: 查询用户状态
    alt 用户有效
        Service->>Database: INSERT Posts
        Database->>Trigger: 触发extract_hashtags
        Trigger->>Database: 提取话题<br/>INSERT Post_Hashtags
        Database->>Service: 调用AnalyzeSentiment
        Service->>Database: 分析情感<br/>INSERT Post_Sentiments
        Service->>Controller: 返回成功
        Controller->>用户: 发布成功
    else 用户无效
        Service->>Controller: 抛出异常
        Controller->>用户: 发布失败
    end
```

#### 流程2：热点话题查询

```mermaid
graph LR
    A["查询请求<br/>时间范围:N天"] -->|SELECT| B["查询Posts<br/>关联Hashtags"]
    B -->|GROUP BY<br/>话题ID| C["统计帖子数<br/>评论数"]
    C -->|计算热度指数<br/>帖子*0.7+评论*0.3| D["排序计算"]
    D -->|ORDER BY DESC<br/>LIMIT 10| E["返回TOP10<br/>热点话题"]
  
    style A fill:#c8e6c9
    style B fill:#a5d6a7
    style C fill:#81c784
    style D fill:#66bb6a
    style E fill:#4caf50
```

#### 流程3：舆情趋势分析

```mermaid
graph LR
    A["查询请求<br/>时间范围"] -->|按日期分组| B["统计日均指标"]
    B -->|计算情感占比| C["正面/中立/负面"]
    C -->|时间序列排序| D["生成趋势数据"]
    D -->|返回结果| E["舆情趋势图"]
  
    style A fill:#bbdefb
    style B fill:#90caf9
    style C fill:#64b5f6
    style D fill:#42a5f5
    style E fill:#2196f3
```

### 2.3 数据流总览

```mermaid
graph TD
    A["社交媒体用户<br/>发布内容"] -->|上传| B["数据管理层<br/>用户、帖子、评论"]
    B -->|自动处理| C["智能分析层<br/>话题提取、情感分析"]
    C -->|持久化| D["GaussDB数据库<br/>存储数据"]
    D -->|查询| E["复杂分析层<br/>热点、趋势、预警"]
    E -->|生成| F["舆情报表<br/>热点话题<br/>趋势分析<br/>预警监测"]
  
    style A fill:#fff9c4
    style B fill:#ffe082
    style C fill:#ffd54f
    style D fill:#ffca28
    style E fill:#fbc02d
    style F fill:#f9a825
```

---

## 三、数据库概念模式设计

### 3.1 实体识别

系统包含**8个核心实体**：

| 实体                | 说明               | 关键属性                           |
| ------------------- | ------------------ | ---------------------------------- |
| **Users**           | 系统用户           | UserID, Username, Email, Status    |
| **Posts**           | 用户发布的帖子     | PostID, UserID, Content            |
| **Comments**        | 帖子下的评论       | CommentID, PostID, UserID, Content |
| **Hashtags**        | 话题标签           | HashtagID, HashtagName             |
| **Post_Hashtags**   | 帖子-话题关联(M:N) | PostID, HashtagID                  |
| **Sentiments**      | 情感倾向类型       | SentimentID, Label                 |
| **Post_Sentiments** | 帖子-情感关联(M:N) | PostID, SentimentID, Score         |
| **Keywords**        | 舆情监测关键词     | KeywordID, Keyword, Category       |

### 3.2 实体属性

**Users实体：** UserID(PK), Username(UK), Email(UK), PasswordHash, Status, CreatedAt, UpdatedAt

**Posts实体：** PostID(PK), UserID(FK), Content, CreatedAt, UpdatedAt

**Comments实体：** CommentID(PK), PostID(FK), UserID(FK), Content, CreatedAt, UpdatedAt

**Hashtags实体：** HashtagID(PK), HashtagName(UK), CreatedAt

**Post_Hashtags关联表：** PostHashtagID(PK), PostID(FK), HashtagID(FK), CreatedAt

**Sentiments实体：** SentimentID(PK), Label(UK), Description

**Post_Sentiments关联表：** PostSentimentID(PK), PostID(FK), SentimentID(FK), Score, CreatedAt

**Keywords实体：** KeywordID(PK), Keyword(UK), Category, CreatedAt

### 3.3 E-R图

```mermaid
erDiagram
    USERS {
        int UserID PK
        string Username UK
        string Email UK
        string PasswordHash
        enum Status
        timestamp CreatedAt
        timestamp UpdatedAt
    }

    POSTS {
        int PostID PK
        int UserID FK
        text Content
        timestamp CreatedAt
        timestamp UpdatedAt
    }

    COMMENTS {
        int CommentID PK
        int PostID FK
        int UserID FK
        text Content
        timestamp CreatedAt
        timestamp UpdatedAt
    }

    HASHTAGS {
        int HashtagID PK
        string HashtagName UK
        timestamp CreatedAt
    }

    POST_HASHTAGS {
        int PostHashtagID PK
        int PostID FK
        int HashtagID FK
        timestamp CreatedAt
    }

    SENTIMENTS {
        int SentimentID PK
        string Label UK
        text Description
    }

    POST_SENTIMENTS {
        int PostSentimentID PK
        int PostID FK
        int SentimentID FK
        decimal Score
        timestamp CreatedAt
    }

    KEYWORDS {
        int KeywordID PK
        string Keyword UK
        string Category
        timestamp CreatedAt
    }

    USERS ||--o{ POSTS : "发布"
    USERS ||--o{ COMMENTS : "发表"
    POSTS ||--o{ COMMENTS : "拥有"
    POSTS }o--|| POST_HASHTAGS : "关联"
    HASHTAGS }o--|| POST_HASHTAGS : "标记"
    POSTS }o--|| POST_SENTIMENTS : "具有"
    SENTIMENTS }o--|| POST_SENTIMENTS : "分类"

```

### 3.4 关系分析

```mermaid
graph TB
    subgraph "核心实体"
        A["Users<br/>用户"]
        B["Posts<br/>帖子"]
        C["Comments<br/>评论"]
    end

    subgraph "标签与分类"
        D["Hashtags<br/>话题"]
        E["Sentiments<br/>情感"]
    end

    subgraph "关联表"
        F["Post_Hashtags<br/>帖子-话题"]
        G["Post_Sentiments<br/>帖子-情感"]
    end

    subgraph "独立表"
        H["Keywords<br/>关键词"]
    end

    A -->|1:N| B
    A -->|1:N| C
    B -->|1:N| C
    B -->|M:N| D
    B -->|M:N| E
    B -.->|通过| F
    B -.->|通过| G
    D -.->|通过| F
    E -.->|通过| G

    style A fill:#c8e6c9
    style B fill:#a5d6a7
    style C fill:#81c784
    style D fill:#bbdefb
    style E fill:#90caf9
    style F fill:#ffe082
    style G fill:#ffcc80
    style H fill:#ffab91
```

---

## 四、数据库逻辑模式设计

### 4.1 关系模式定义

```mermaid
graph LR
    A["Users<br/>PK: UserID<br/>UK: Username,Email"] -->|1:N| B["Posts<br/>PK: PostID<br/>FK: UserID"]
    A -->|1:N| C["Comments<br/>PK: CommentID<br/>FK: PostID,UserID"]
    B -->|M:N| D["Hashtags<br/>PK: HashtagID<br/>UK: HashtagName"]
    B -->|M:N| E["Sentiments<br/>PK: SentimentID<br/>UK: Label"]
  
    F["Post_Hashtags<br/>FK: PostID,HashtagID"] -.->|关联| B
    F -.->|关联| D
  
    G["Post_Sentiments<br/>FK: PostID,SentimentID"] -.->|关联| B
    G -.->|关联| E
  
    H["Keywords<br/>PK: KeywordID<br/>UK: Keyword"] -.->|独立| B
  
    style A fill:#c8e6c9
    style B fill:#a5d6a7
    style C fill:#81c784
    style D fill:#bbdefb
    style E fill:#90caf9
    style F fill:#ffe082
    style G fill:#ffcc80
    style H fill:#ffab91
```

### 4.2 范式验证

#### 第一范式(1NF)

✓ **所有关系均满足1NF** - 所有属性值均为原子值，不存在重复组

```mermaid
graph LR
    A["检查属性原子性"] -->|✓| B["Users表"]
    A -->|✓| C["Posts表"]
    A -->|✓| D["Comments表"]
    A -->|✓| E["Hashtags表"]
    A -->|✓| F["关联表"]
  
    B --> G["所有属性均为原子值<br/>无重复组"]
    C --> G
    D --> G
    E --> G
    F --> G
  
    G --> H["结论: 全部满足1NF"]
  
    style H fill:#c8e6c9,font-weight:bold
```

#### 第二范式(2NF)

✓ **所有关系均满足2NF** - 非主键属性完全依赖于主键

```mermaid
graph TB
    A["范式检查"] -->|Users| B["所有属性完全<br/>依赖于UserID ✓"]
    A -->|Posts| C["所有属性完全<br/>依赖于PostID ✓"]
    A -->|Comments| D["所有属性完全<br/>依赖于CommentID ✓"]
    A -->|关联表| E["属性依赖于<br/>复合主键 ✓"]
  
    B --> F["结论: 全部满足2NF"]
    C --> F
    D --> F
    E --> F
  
    style F fill:#81c784,color:#fff,font-weight:bold
```

#### 第三范式(3NF)

✓ **所有关系均满足3NF** - 不存在非主键属性间的传递依赖

```mermaid
graph TD
    A["检查传递依赖"] -->|Users| B["Username → Email?<br/>No ✓"]
    A -->|Posts| C["UserID → Content?<br/>No ✓"]
    A -->|Comments| D["PostID → UserID?<br/>No ✓"]
    A -->|关联表| E["最小化设计 ✓"]
  
    B --> F["不存在非主键属性间<br/>的传递依赖"]
    C --> F
    D --> F
    E --> F
  
    F --> G["结论: 全部满足3NF<br/>数据结构最优<br/>消除冗余"]
  
    style G fill:#4caf50,color:#fff,font-weight:bold
```

**3NF验证总结：**

| 表              | 传递依赖分析              | 结论  |
| --------------- | ------------------------- | ----- |
| Users           | 各属性独立，无依赖链      | ✓ 3NF |
| Posts           | UserID和Content无依赖关系 | ✓ 3NF |
| Comments        | 外键和属性独立            | ✓ 3NF |
| Hashtags        | 单一属性，无传递          | ✓ 3NF |
| Post_Hashtags   | 最小化，仅主键和时间戳    | ✓ 3NF |
| Post_Sentiments | Score与主键直接相关       | ✓ 3NF |
| Keywords        | 独立设计，无传递依赖      | ✓ 3NF |

### 4.3 约束完整性

```mermaid
graph LR
    A["数据库约束"] -->|主键约束| B["每表唯一标识"]
    A -->|外键约束| C["引用完整性<br/>级联删除"]
    A -->|唯一约束| D["Username、Email<br/>HashtagName唯一"]
    A -->|非空约束| E["关键字段必填"]
  
    style A fill:#fff9c4
    style B fill:#ffe082
    style C fill:#ffd54f
    style D fill:#ffca28
    style E fill:#fbc02d
```

### 4.4 复杂查询设计

```mermaid
graph LR
    A["热点话题排行<br/>过去N天热度TOP10"] --> B["Posts+Hashtags+Comments<br/>GROUP BY 话题<br/>热度=帖子*0.7+评论*0.3"]
  
    C["舆情趋势分析<br/>按时间序列显示趋势"] --> D["Posts+Post_Sentiments<br/>GROUP BY 日期<br/>统计各情感占比"]
  
    E["情感分布分析<br/>统计正/中/负占比"] --> F["Post_Sentiments+Sentiments<br/>COUNT 各类型<br/>计算百分比"]
  
    G["关键词预警<br/>24小时内敏感词"] --> H["Posts+Comments<br/>模糊匹配关键词<br/>时间范围筛选"]
  
    style A fill:#c8e6c9
    style B fill:#a5d6a7
    style C fill:#bbdefb
    style D fill:#90caf9
    style E fill:#ffe082
    style F fill:#ffcc80
    style G fill:#ffab91
    style H fill:#ff9375
```

## 五、后续规划

### 5.1 核心待办事项

```mermaid
mindmap
  root((后续任务))
    数据库实现
      建表与索引
      触发器开发
      存储过程实现
      复杂查询优化
    测试验证
      测试数据准备
      功能测试
      性能测试
    文档编写
      系统设计报告
      用户手册
```

### 5.2 具体任务清单

#### **数据库实现**
- [ ] 在GaussDB中创建8个核心表
- [ ] 建立主键、外键和唯一约束
- [ ] 创建必要的索引
- [ ] 实现话题提取触发器
- [ ] 实现情感分析存储过程
- [ ] 优化4个复杂查询性能

#### **测试验证**
- [ ] 准备完整的测试数据集
- [ ] 验证所有CRUD操作
- [ ] 测试触发器功能
- [ ] 验证存储过程逻辑
- [ ] 执行复杂查询性能测试

#### **文档编写**
- [ ] 完善系统设计报告
- [ ] 编写用户操作手册
- [ ] 整理SQL脚本文档
- [ ] 准备演示材料

---
